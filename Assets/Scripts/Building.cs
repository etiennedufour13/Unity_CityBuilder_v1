using UnityEngine;

public class Building : MonoBehaviour
{
    public bool IsValidPlacement()
    {
        // Ajoute ici la logique pour éviter le chevauchement, vérifier le terrain, etc.
        return true;
    }
}
